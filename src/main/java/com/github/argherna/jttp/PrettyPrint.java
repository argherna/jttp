package com.github.argherna.jttp;

/**
 * Describes pretty print options.
 */
enum PrettyPrint {
    /** No options applied. */
    NONE,
    /** Color output only. */
    COLORS,
    /** Indented output only. */
    INDENT,
    /** Color and indent. */
    ALL;
}